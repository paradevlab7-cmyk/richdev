# 나라장터 API 조사 기록

## 공식 확인 사항

- 공공데이터포털의 조달청_나라장터 공공데이터개방표준서비스 상세 페이지(https://www.data.go.kr/data/15058815/openapi.do)는 REST 방식이며 JSON과 XML을 제공한다.
- 해당 서비스는 나라장터의 입찰·낙찰·계약정보를 입찰공고일시, 개찰일시, 계약체결일자 기준으로 제공하며, 개별 입찰·낙찰·계약 API도 별도로 제공한다고 안내한다.
- 개방표준서비스의 계약정보 오퍼레이션은 응답속도·부하 개선을 위해 조회기간을 기존 1개월에서 1주일로 축소 운영한다고 안내되어 있다. 따라서 수집기는 짧은 기간 단위로 분할 호출하고 재시도·중복 제거해야 한다.
- 공공데이터포털 서비스의 수정일은 2026-06-29이며, 개발계정 신청 가능 트래픽은 10,000으로 표시되어 있다.
- 조달청 공지(https://www.data.go.kr/bbs/ntc/selectNotice.do?originId=NOTICE_0000000004509)는 2026-02-27부터 개방표준서비스의 통합계약번호(untyCntrctNo)를 계약번호(cntrctNo)와 동일한 데이터로 변경한다고 안내한다. 데이터 모델은 변경 이후 필드를 우선 사용하고 기존 응답도 방어적으로 처리한다.

## 구현 원칙

- 사용자가 지정한 정확한 5개 기본 URL을 코드의 API 카탈로그에 그대로 보존한다.
- 실제 오퍼레이션명과 필드명은 공공데이터포털 참고문서에 맞춘 서비스별 어댑터에서 관리한다.
- 인증키·텔레그램 토큰·사용자 ID는 환경변수나 소스 코드에 넣지 않고 설정 화면에서 암호화된 사용자 설정으로 관리한다.
- 1시간 수집과 08:00 알림은 애플리케이션 내부 타이머가 아니라 플랫폼 Heartbeat의 /api/scheduled/* 엔드포인트로 구현한다.

## 사전규격정보서비스 추가 확인 — 2026-08-15

- 기본 URL은 `https://apis.data.go.kr/1230000/ao/HrcspSsstndrdInfoService`다.
- 기존 `getPublicPrcureThngInfoServc`는 단일 용역 오퍼레이션이어서 전체 사전규격 수집에 적합하지 않다.
- 통합 검색용 우선 오퍼레이션은 `getPublicPrcureThngInfoServcPPSSrch`이며, `inqryDiv=1`, `inqryBgnDt`, `inqryEndDt`를 함께 전달한다.
- 사전규격은 물품·용역·공사별 오퍼레이션이 존재하므로, PPSSrch 결과가 없는 경우 카테고리별 오퍼레이션을 병합하는 보완이 필요하다.
