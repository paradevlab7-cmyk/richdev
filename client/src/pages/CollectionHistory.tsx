import { useState } from "react";
import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Progress } from "@/components/ui/progress";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, Clock3, Database, FileWarning, XCircle } from "lucide-react";

function formatNumber(value: number | null | undefined) { return Number(value ?? 0).toLocaleString("ko-KR"); }
function statusMeta(status: "running" | "success" | "failed") {
  if (status === "success") return { label: "완료", icon: CheckCircle2, className: "bg-emerald-100 text-emerald-800 dark:bg-emerald-950/50 dark:text-emerald-300" };
  if (status === "failed") return { label: "실패", icon: XCircle, className: "bg-red-100 text-red-800 dark:bg-red-950/50 dark:text-red-300" };
  return { label: "순차 수집 중", icon: Clock3, className: "bg-amber-100 text-amber-800 dark:bg-amber-950/50 dark:text-amber-300" };
}

export default function CollectionHistory() {
  const { data: runs = [], isLoading } = trpc.collection.runs.useQuery(undefined, { refetchInterval: 15000 });
  const [selectedError, setSelectedError] = useState<{ source: string; message: string; startedAt: Date | string | number } | null>(null);
  return <DashboardLayout><div className="min-h-screen -m-3 bg-background p-3 sm:-m-5 sm:p-5 lg:-m-6 lg:p-6"><div className="mx-auto max-w-[1440px]"><div className="mb-6"><p className="text-xs font-semibold tracking-[.16em] text-primary">COLLECTION HISTORY</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">수집 이력</h1><p className="mt-2 text-muted-foreground">API 전체 결과, 실제 저장된 공고, 페이지 진행 상태를 비교합니다. 실행 중인 수집은 15초마다 자동 갱신됩니다.</p></div><Card className="app-surface border-0"><CardHeader className="flex-row items-start justify-between gap-4"><div><CardTitle className="flex items-center gap-2 text-base"><Database className="h-4 w-4 text-primary" />최근 수집 실행</CardTitle><p className="mt-1 text-xs text-muted-foreground">저장 건수는 동일 공고 갱신을 제외한 현재 기간 내 DB 보유 건수입니다.</p></div><Badge variant="outline">최근 10회</Badge></CardHeader><CardContent className="space-y-4">{isLoading && <p className="py-8 text-center text-sm text-muted-foreground">수집 이력을 불러오는 중입니다.</p>}{!isLoading && !runs.length && <p className="py-8 text-center text-sm text-muted-foreground">아직 수집 이력이 없습니다.</p>}{runs.map(run => { const status = statusMeta(run.status); const StatusIcon = status.icon; const total = Number(run.totalAvailable ?? 0); const stored = Number(run.storedCount ?? 0); const fetched = Number(run.fetchedCount ?? 0); const progress = total ? Math.min(100, Math.round(fetched / total * 100)) : 0; return <div key={run.id} className="soft-panel p-4"><div className="flex flex-col justify-between gap-3 md:flex-row md:items-center"><div className="flex flex-wrap items-center gap-2"><Badge variant="secondary" className="capitalize">{run.sourceType}</Badge><Badge className={status.className}><StatusIcon className={`mr-1 h-3.5 w-3.5 ${run.status === "running" ? "animate-pulse" : ""}`} />{status.label}</Badge>{run.isBackground && <Badge variant="outline">백그라운드</Badge>}</div><p className="text-xs text-muted-foreground">{new Date(run.startedAt).toLocaleString("ko-KR")}</p></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-xs text-muted-foreground">API 전체 건수</p><p className="mt-1 text-lg font-semibold">{total ? `${formatNumber(total)}건` : "미확인"}</p></div><div><p className="text-xs text-muted-foreground">실제 저장 건수</p><p className="mt-1 text-lg font-semibold">{stored ? `${formatNumber(stored)}건` : "집계 중"}</p></div><div><p className="text-xs text-muted-foreground">수집 페이지</p><p className="mt-1 text-lg font-semibold">{run.currentPage || 0} / {run.totalPages || "-"}</p></div><div><p className="text-xs text-muted-foreground">이번 실행 수집</p><p className="mt-1 text-lg font-semibold">{formatNumber(fetched)}건</p></div></div>{total > 0 && <div className="mt-4"><div className="mb-2 flex justify-between text-xs text-muted-foreground"><span>진행률</span><span>{progress}% · 수집 {formatNumber(fetched)} / {formatNumber(total)}</span></div><Progress value={progress} className={`h-2 overflow-hidden bg-muted [&>div]:bg-gradient-to-r [&>div]:from-primary [&>div]:to-cyan-500 [&>div]:transition-all [&>div]:duration-700 ${run.status === "running" ? "animate-pulse" : ""}`} /></div>}{run.errorMessage && <div className="mt-3 flex items-center justify-between gap-3 rounded-lg border border-red-200/70 bg-red-50/70 p-3 text-xs text-red-800 dark:border-red-950/70 dark:bg-red-950/30 dark:text-red-200"><span className="line-clamp-1">{run.errorMessage}</span><Button variant="ghost" size="sm" className="shrink-0 text-red-700 hover:text-red-900 dark:text-red-200" onClick={() => setSelectedError({ source: run.sourceType, message: run.errorMessage!, startedAt: run.startedAt })}><FileWarning className="mr-1.5 h-3.5 w-3.5" />오류 로그</Button></div>}</div>; })}</CardContent></Card></div><Dialog open={!!selectedError} onOpenChange={open => !open && setSelectedError(null)}><DialogContent className="max-w-xl"><DialogHeader><DialogTitle>수집 오류 상세</DialogTitle><DialogDescription>{selectedError ? `${selectedError.source} · ${new Date(selectedError.startedAt).toLocaleString("ko-KR")}` : ""}</DialogDescription></DialogHeader><pre className="max-h-72 overflow-auto rounded-lg border bg-muted/60 p-4 text-xs leading-5 text-foreground whitespace-pre-wrap break-words">{selectedError?.message}</pre></DialogContent></Dialog></div></DashboardLayout>;
}
